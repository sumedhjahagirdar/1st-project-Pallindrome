# 1st-project-Pallindrome
I created a program in C that takes a number as input and checks whether it is a palindrome, meaning the number reads the same forward and backward, and then returns the result accordingly.
